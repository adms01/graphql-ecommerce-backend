export declare class Product {
    id: string;
    createdAt: Date;
    name: string;
    description: string;
    price: number;
    images: string;
    constructor(name?: string, description?: string, price?: number, images?: string);
}
